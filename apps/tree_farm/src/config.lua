M = {}

M.config = {
    max_y = 100,
    max_x = 100,
    fuel_threshold = 20000,
    modem_direction = "away_x",
    routine_protocol = "tree_farm_coy1",
    -- swarm_count = 22,
    swarm_count = 2,
}

return M.config